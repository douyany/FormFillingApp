The 
text_listing_of_docvariables
lists the document fields that appear in the Word document

The
templateyness.docx
is a Word document that has the document fields laid out very simply, so that it's easy to check whether the form is working.

The best version so far was having the image of the form as the background and then using fields to complete the parts of the form.

The files that end with "as_watermark_uses_arial.doc" are the ones that App_to_Fill_FamEngmtStudyForms_jan21.docm feeds

The instructions document instructions_on_how_to_use_the_app.docx provides more formal directions.